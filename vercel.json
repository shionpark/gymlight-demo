{
  "framework": null,
  "installCommand": "corepack enable && yarn install --immutable",
  "buildCommand": "yarn workspace gymlight-manager build:demo",
  "outputDirectory": "packages/gymlight-manager/dist"
}
