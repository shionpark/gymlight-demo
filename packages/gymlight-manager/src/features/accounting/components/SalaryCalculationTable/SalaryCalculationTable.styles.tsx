import styled from '@emotion/styled';

import { DualSideBar } from 'gymlight-design-system';

export const TableInfoDualSideBar = styled(DualSideBar)`
  padding: 0rem 2rem;

  & button {
    width: 3.3rem;
    height: 2rem;
    margin-bottom: 0.25rem;
  }

  .left-section {
    gap: 0.25rem;
  }
`;
